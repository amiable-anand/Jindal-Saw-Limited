<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Jindal.Models"
             x:Class="Jindal.Views.LocationPage"
             Title="Location Master"
             BackgroundColor="#F8FAFC">

    <VerticalStackLayout Padding="20" Spacing="16">

        <!-- ?? Header: Add, Search, Reload -->
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- ? Add Button -->
            <Button Text="+ Add Location"
                    Grid.Column="0"
                    Clicked="OnAddLocationClicked"
                    BackgroundColor="#059669"
                    TextColor="White"
                    FontSize="13"
                    FontAttributes="Bold"
                    CornerRadius="8"
                    Padding="10,4"
                    WidthRequest="130"
                    HeightRequest="36" />

            <!-- ?? Search -->
            <SearchBar x:Name="SearchBar"
                       Grid.Column="1"
                       Placeholder="Search location..."
                       SearchButtonPressed="OnSearchClicked"
                       FontSize="13"
                       BackgroundColor="#F3F4F6"
                       PlaceholderColor="#64748B"
                       TextColor="#374151"
                       Margin="8,0"
                       HeightRequest="40" />

            <!-- ?? Reload -->
            <Button Text="Reload"
                    Grid.Column="2"
                    Clicked="OnReloadClicked"
                    BackgroundColor="#E5E7EB"
                    TextColor="#374151"
                    FontSize="13"
                    FontAttributes="Bold"
                    CornerRadius="8"
                    Padding="10,4"
                    WidthRequest="80"
                    HeightRequest="36" />
        </Grid>

        <!-- ?? Table Headers -->
        <Border BackgroundColor="White" StrokeShape="RoundRectangle 12" Padding="16" Shadow="{Shadow Brush=Gray, Offset='2,2', Radius=4, Opacity=0.3}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Label Text="Name" FontAttributes="Bold" TextColor="#1E3A8A" FontSize="13"/>
                <Label Text="Code" Grid.Column="1" FontAttributes="Bold" TextColor="#1E3A8A" FontSize="13"/>
                <Label Text="Address" Grid.Column="2" FontAttributes="Bold" TextColor="#1E3A8A" FontSize="13"/>
                <Label Text="Remark" Grid.Column="3" FontAttributes="Bold" TextColor="#1E3A8A" FontSize="13"/>
                <Label Text="Action" Grid.Column="4" FontAttributes="Bold" TextColor="#1E3A8A" FontSize="13"/>
            </Grid>
        </Border>

        <!-- ?? Location List -->
        <CollectionView x:Name="LocationCollection" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Location">
                    <Border BackgroundColor="White"
                           StrokeShape="RoundRectangle 8"
                           Padding="12"
                           Margin="0,5"
                           Shadow="{Shadow Brush=Gray, Offset='1,1', Radius=3, Opacity=0.2}">

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2*" />
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Name}" TextColor="#374151" FontSize="13"/>
                            <Label Text="{Binding LocationCode}" Grid.Column="1" TextColor="#374151" FontSize="13"/>
                            <Label Text="{Binding Address}" Grid.Column="2" TextColor="#374151" FontSize="13"/>
                            <Label Text="{Binding Remark}" Grid.Column="3" TextColor="#374151" FontSize="13"/>

                            <!-- ?? Edit & ? Delete Actions -->
                            <HorizontalStackLayout Grid.Column="4" Spacing="4">
                                <Button Text="Edit"
                                        Clicked="OnEditClicked"
                                        BackgroundColor="#1E3A8A"
                                        TextColor="White"
                                        FontSize="12"
                                        FontAttributes="Bold"
                                        CornerRadius="6"
                                        Padding="8,3"
                                        WidthRequest="60"
                                        HeightRequest="30" />

                                <Button Text="Delete"
                                        Clicked="OnDeleteClicked"
                                        BackgroundColor="#DC2626"
                                        TextColor="White"
                                        FontSize="12"
                                        FontAttributes="Bold"
                                        CornerRadius="6"
                                        Padding="8,3"
                                        WidthRequest="60"
                                        HeightRequest="30" />
                            </HorizontalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
